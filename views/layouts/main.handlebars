<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <link rel="stylesheet" href="/public/main_styles.css">
    <title>{{title}}</title>
</head>
<body>
    <header>
        <div class="topnav">
            <a class="active" href="/">Home</a>
            {{#if signed_in}}
            <a href="/clothes">My Clothes</a>
            <a href="/">My Outfits</a>
            <a href="/">Statistics</a>
            <a href="/">Profile</a>
            {{/if}}
            {{#if home_not_signed_in}}
            <a href="/">Login/Signup</a>
            {{/if}}
        </div>
    </header>
    
    <main>
    {{{body}}}
    </main>
</body>
</html>